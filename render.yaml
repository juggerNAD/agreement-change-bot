services:
  - type: worker
    name: agreement-change-bot
    env: docker
    plan: starter
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: GROUP_CHAT_ID
        sync: false
      - key: APPS_SCRIPT_URL
        sync: false
