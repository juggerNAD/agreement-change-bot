services:
  - type: web
    name: agreement-change-bot
    env: docker
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: GROUP_CHAT_ID
        sync: false
      - key: APPS_SCRIPT_URL
        sync: false
